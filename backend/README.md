# Tally2Books Backend Agent

This folder contains the Python sync agent used to connect Tally data with the Tally2Books API.

## Run

Install Python dependencies:

```bash
pip install -r requirements.txt
```

Start the agent:

```bash
npm start
```

or:

```bash
python python_agent.py
```
